// Shared app version constant — update here and in android/app/build.gradle
// v2.10.46: Coffee (orgtype='C') — POST /api/milk-collection now writes season SCODE to transactions.session (matches CAN column) instead of full season descript. Dairy unchanged.
export const APP_VERSION = '2.10.46';
export const APP_VERSION_CODE = 68;
