// Shared app version constant — update here and in android/app/build.gradle
// v2.10.3: Fix verification using wrong field name (memberno → farmer_id) causing valid syncs to stay pending
export const APP_VERSION = '2.10.3';
export const APP_VERSION_CODE = 34;
